apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stalwart-mail
  namespace: stalwart-mail
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-ephemeral
